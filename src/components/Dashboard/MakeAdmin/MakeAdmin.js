import { Box, Typography } from "@mui/material";
import React from "react";

const MakeAdmin = () => {
  return (
    <Box>
      <Typography
        color="error"
        sx={{ textAlignment: "center", fontSize: "36px" }}
      >
        Need Some time to complete this feature.
      </Typography>
    </Box>
  );
};

export default MakeAdmin;
